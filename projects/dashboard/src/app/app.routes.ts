import { Routes } from '@angular/router';

// M31 is infrastructure only. Dashboard shell, lazy feature modules, and
// nav land in M36+. Keeping this empty intentionally.
export const routes: Routes = [];
